{
    "name": "static-site",
    "version": "1.0.0",
    "description": "Command-line application that uses input from a user to dynamically generate a README for a GitHub project.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Brandonespencer",
    "license": "ISC",
    "dependencies": {
        "axios": "^0.21.2",
        "inquirer": "^8.2.0"
    }
}
